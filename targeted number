def twoSum(nums, target):
   l=len(nums)
   for i in range(l):
      for j in range(i+1,l):
         if(nums[i]+nums[j]==target):
            return (i,j)
         else:
            continue
num=eval(input("enter a list:"))
tar=int(input("enter a targeted number:"))
print(twoSum(num,tar))
